# ⚠️ Configuration DÉVELOPPEMENT uniquement
# Pour les tests, voir .env.test (PostgreSQL Docker local)

TZ=UTC
PORT=3333
HOST=localhost
LOG_LEVEL=info
APP_KEY=
NODE_ENV=development

# Base de données - SUPABASE CLOUD ou PostgreSQL local dev
# ⚠️ NE JAMAIS utiliser ces credentials pour les tests !
# Les tests utilisent .env.test avec Docker PostgreSQL (port 5433)
DB_HOST=your-project.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-password
DB_DATABASE=postgres

# Optional: App user credentials (non-superuser for RLS enforcement)
# For LOCAL DOCKER development only - enables RLS like production
# If using Supabase cloud, leave these empty (Supabase handles roles)
# If using Docker postgres-dev, uncomment to enforce RLS during development:
#DB_APP_USER=app_user
#DB_APP_PASSWORD=app_password

SESSION_DRIVER=cookie

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:3000

# OAuth Providers (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3333/api/v1/auth/oauth/google/callback

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:3333/api/v1/auth/oauth/github/callback

# Seed users passwords (development only)
SEED_ADMIN_PASSWORD=your-admin-password
SEED_USER_PASSWORD=your-user-password

# Resend Email Service
# Get your API key from https://resend.com/api-keys
#RESEND_API_KEY=re_xxxxxxxxxx
#MAIL_FROM_ADDRESS=noreply@yourdomain.com
#MAIL_FROM_NAME=Your App Name

# Payment Provider Selection
# Which payment provider to use: stripe, paddle, lemonsqueezy, polar
# Only one provider is active at a time. Default: stripe
PAYMENT_PROVIDER=stripe

# Stripe Payment Provider
# Get your keys from https://dashboard.stripe.com/apikeys
#STRIPE_SECRET_KEY=sk_test_xxx
#STRIPE_WEBHOOK_SECRET=whsec_xxx
#STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Paddle Payment Provider
# Get your keys from https://vendors.paddle.com/account
#PADDLE_API_KEY=
#PADDLE_WEBHOOK_SECRET=
#PADDLE_CLIENT_TOKEN=
#PADDLE_ENVIRONMENT=sandbox

# LemonSqueezy Payment Provider
# Get your keys from https://app.lemonsqueezy.com/settings/api
#LEMONSQUEEZY_API_KEY=
#LEMONSQUEEZY_WEBHOOK_SECRET=
#LEMONSQUEEZY_STORE_ID=

# Polar Payment Provider
# Get your keys from https://dashboard.polar.sh/settings
#POLAR_ACCESS_TOKEN=
#POLAR_WEBHOOK_SECRET=
#POLAR_ORGANIZATION_ID=

# At-Rest Encryption Key (for sensitive data like SSO secrets)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Falls back to APP_KEY if not set
# For key rotation, use comma-separated: "newKey,oldKey"
#ENCRYPTION_MASTER_KEY=
